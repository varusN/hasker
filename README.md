# hasker

Q&A service:
Web-Framework: Django
Database: MySQL 
Protocol: uwsgi
Container: docker

Linux based installation:

git clone https://github.com/varusN/hasker.git
cd hasker/
make prod

Server will run on 
http://<serverIP>:8080
